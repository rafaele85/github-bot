overwrite: true
schema: "https://api-dev.ppl.do/graphql"
documents: null
generates:
  src/components/chat-service/schema/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
